@model IEnumerable<ITSM.ViewModels.TicketCreateViewModel>

@{
    ViewData["Title"] = "All Tickets";
}

<h1>@ViewData["Title"]</h1>

<table class="table">
    <thead>
    <tr>
        <th>Номер</th>
        <th>Title</th>
        <th>Description</th>
        <th>Created At</th>
        <th>Status</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var ticket in Model)
    {
        <tr>
            <td>@ticket.Id</td>
            <td>@ticket.Title</td>
            <td>@ticket.Description</td>
            <td>@ticket.CreatedAt</td>
            <td>@ticket.Status</td>
        </tr>
    }
    </tbody>
</table>