@model ITSM.ViewModels.Create.SubCategoryCreateViewModel

@{
  
    var category = (ITSM.Models.TicketCategory)ViewData["Category"]!;
    ViewData["Title"] = category.Name;
}

<h2 class="text-center mb-4">@ViewData["Title"]</h2>


<div class="table-responsive shadow-sm rounded">
    <table class="table table-hover table-bordered align-middle text-center">
        <thead class="table-dark text-white">
            <tr>
                <th>Subcategory Title</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody class="table-light">
            @foreach (var subCategory in category.SubCategories)
            {
                <tr>
                    <td>@subCategory.Name</td>
                    <td>
                        <form asp-controller="TicketCategory" asp-action="DeleteSubCategory" asp-route-subCategoryId="@subCategory.Id" method="post" onsubmit="return confirm('Are you sure you want to delete this subcategory?');">
                            <button type="submit" class="btn btn-outline-danger btn-sm">Delete</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>


<div class="text-center mt-3">
    <form asp-controller="TicketCategory" asp-action="AddSubCategory" method="post" class="col-md-6 offset-md-3">
        <input type="hidden" asp-for="CategoryId" />

        <div class="mb-3">
            <label asp-for="Name" class="form-label">Subcategory Name</label>
            <input asp-for="Name" class="form-control" />
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>

        <button type="submit" class="btn btn-outline-success w-100">Add Subcategory</button>
    </form>
</div>

<div class="text-center mt-3">
    <a asp-controller="TicketCategory" asp-action="CategoryList" class="btn btn-outline-secondary w-100">Back to Categories</a>
</div>

